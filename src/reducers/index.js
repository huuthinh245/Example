import { movieReducer as movie} from './movieReducer';
import { userReducer as user} from './userReducer';
import { mapReducer as map} from './mapReducer';
export default {
    movie,
    user,
    map
}